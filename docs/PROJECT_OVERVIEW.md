# Обзор проекта

`ai-editor` — локальный desktop-редактор для русскоязычного текста с подсказками во время набора.

Пользователь пишет текст, видит короткие подсказки рядом с курсором, выбирает их стрелками вверх/вниз и принимает через `Tab`. Подсказка может быть словом, фразой или коротким продолжением предложения.

## Для кого

Проект ориентирован на людей, которые часто пишут русские тексты и хотят лёгкий локальный инструмент без аккаунтов и облачной синхронизации: заметки, черновики, рабочие тексты, личные документы.

Редактор не должен превращаться в чат-бота, IDE, админку или облачный сервис.

## Почему локально

Локальная модель развития выбрана ради приватности и предсказуемости:

- пользовательский текст не уходит во внешний сервис;
- приложение может работать без аккаунта;
- интеграции можно развивать постепенно и изолированно;
- mock/dictionary providers позволяют делать MVP без установленной Ollama.

## Почему Tauri

Tauri даёт лёгкую desktop-оболочку, React/TypeScript UI и возможность держать Rust-часть минимальной. Нативный слой нужен только там, где frontend не справляется сам.

## Ollama

Ollama рассматривается как локальный inference backend для MVP 2. В MVP 1 нужна архитектура, которая позволит подключить `OllamaSuggestionProvider` позже без переписывания UI.

## Не входит сейчас

- реальная Ollama-интеграция;
- скачивание моделей;
- GPU/CPU detection;
- production installer;
- аккаунты и облачная синхронизация;
- telemetry и аналитика;
- полноценный менеджер файлов;
- система плагинов;
- llama.cpp adapter.
